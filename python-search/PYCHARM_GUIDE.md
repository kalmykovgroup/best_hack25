# Как запустить gRPC сервис в PyCharm

## Шаг 1: Открыть проект в PyCharm

1. Откройте PyCharm
2. Выберите **File → Open**
3. Перейдите в папку: `C:\Users\Apolon 1\RiderProjects\best_hack25\python-search`
4. Нажмите **OK**

## Шаг 2: Установить зависимости

### Способ 1: Через терминал PyCharm (рекомендуется)

1. Откройте терминал в PyCharm: **View → Tool Windows → Terminal** (или Alt+F12)
2. Активируйте виртуальное окружение (если не активировано автоматически):
```bash
.venv\Scripts\activate
```
3. Выполните команду:
```bash
pip install grpcio grpcio-tools protobuf
```

**Важно**: Если у вас Python 3.13, используйте команду выше вместо `pip install -r requirements.txt`

### Способ 2: Через Python Packages

1. В PyCharm внизу нажмите на вкладку **Python Packages**
2. В поиске введите и установите:
   - `grpcio`
   - `grpcio-tools`
   - `protobuf`

## Шаг 3: Сгенерировать gRPC код

В терминале PyCharm выполните:
```bash
python generate_grpc.py
```

Вы должны увидеть:
```
✅ Файлы успешно сгенерированы:
   - geocode_pb2.py
   - geocode_pb2_grpc.py
```

## Шаг 4: Запустить сервер

### Способ 1: Через правый клик (самый простой)

1. В проводнике проекта (слева) найдите файл `grpc_server.py`
2. Нажмите на него **правой кнопкой мыши**
3. Выберите **Run 'grpc_server'**

### Способ 2: Через кнопку запуска

1. Откройте файл `grpc_server.py`
2. Нажмите зеленую стрелку (▶) вверху справа
3. Выберите **Run 'grpc_server'**

### Способ 3: Через терминал

```bash
python grpc_server.py
```

## Шаг 5: Проверить что сервер работает

После запуска вы должны увидеть в консоли:
```
✅ Python gRPC сервер запущен на порту 50051
📡 Endpoints:
   - SearchAddress (поиск адресов)
   - HealthCheck (проверка здоровья)
Ожидание запросов...
```

## Остановка сервера

### В PyCharm:

1. Нажмите красную кнопку **Stop** (⬛) в панели запуска (обычно внизу)
2. Или нажмите **Ctrl+F2**

### В терминале:

- Нажмите **Ctrl+C**

## Перезапуск сервера

1. Остановите сервер (см. выше)
2. Запустите снова через правый клик или кнопку запуска
3. Или используйте **Ctrl+F5** (Rerun)

## Проверка работы через grpcurl (опционально)

Если у вас установлен grpcurl, можно протестировать:

```bash
# Health check
grpcurl -plaintext localhost:50051 geocode.GeocodeService/HealthCheck

# Поиск адреса
grpcurl -plaintext -d "{\"normalized_query\": \"Москва\", \"limit\": 5, \"request_id\": \"test\"}" localhost:50051 geocode.GeocodeService/SearchAddress
```

## Частые проблемы

### Ошибка: "ModuleNotFoundError: No module named 'geocode_pb2'"
**Решение**: Выполните `python generate_grpc.py`

### Ошибка: "Address already in use"
**Решение**: Порт 50051 уже занят. Остановите предыдущий запущенный сервер.

### Ошибка: "pip: command not found"
**Решение**: Убедитесь что Python установлен и добавлен в PATH. Или используйте `python -m pip install -r requirements.txt`

## Горячие клавиши PyCharm

- **Shift+F10** - Запустить последнюю конфигурацию
- **Ctrl+F5** - Перезапустить
- **Ctrl+F2** - Остановить
- **Alt+F12** - Открыть терминал

## Настройка конфигурации запуска (опционально)

1. **Run → Edit Configurations**
2. Нажмите **+** → **Python**
3. Настройте:
   - **Name**: `gRPC Server`
   - **Script path**: `C:\Users\Apolon 1\RiderProjects\best_hack25\python-search\grpc_server.py`
   - **Working directory**: `C:\Users\Apolon 1\RiderProjects\best_hack25\python-search`
4. Нажмите **OK**

Теперь вы можете запускать через выбор этой конфигурации вверху.